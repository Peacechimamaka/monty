#include "monty.h"

/**
 *nop - prints all the ints in the stack
 *
 *@h: head
 *
 *@count: line count
 *
 * Return: Nothing
 */

void i_nop(stack_t **h, unsigned int count)
{
        (void) count;
        (void) (*h);
}
